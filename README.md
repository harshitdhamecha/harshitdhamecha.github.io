# Harshit Dhamecha — personal portfolio

A responsive single-page personal website built from the provided resume.

To preview locally from this directory:

```sh
python3 -m http.server 4173
```

Then visit `http://localhost:4173`.
