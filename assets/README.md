# assets

Drop `shady.mp3` here (a legally obtained copy of the track you want the
turntable to play) and the deck plays it from the timestamp configured as
`SONG_START` in `turntable.js`. No audio ships with this repo — without the
file the deck uses a fully synthesized beat.
